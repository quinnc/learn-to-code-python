import math

print (math.factorial(5))
print (math.ceil(2.2))

radius = float ( input ("Enter the circle's radius: ") )

print ("A circle of radius", radius, "has a circumference of", round(2*math.pi*radius, 2), "and an area of", round(math.pi*radius**2, 2))
